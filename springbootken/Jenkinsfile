pipeline {
    agent { docker {
    	image 'maven:3.3.3'
    	args '-u root:sudo' } }
    stages {
        stage('build') {
            steps {
            	dir('springbootken') {
            		sh 'sudo ./mvnw clean spring-boot:run'
            	}
            }
        }
    }
}