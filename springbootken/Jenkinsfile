pipeline {
    agent { docker { image 'maven:3.3.3' } }
    stages {
        stage('build') {
            steps {
            	sh 'cd springbootken'
            	sh 'ls'
                sh './mvnw clean spring-boot:run'
            }
        }
    }
}