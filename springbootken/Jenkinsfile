pipeline {
    agent { docker {
    	image 'maven:3.3.3'
    	args '-u root:root' } }
    stages {
        stage('build') {
            steps {
            	dir('springbootken') {
            		sh './mvnw clean spring-boot:run'
            	}
            }
        }
    }
}